# LaTeX PDF Bridge

A tiny local extension that opens the current TeX file's PDF with `vscode-pdf` (`pdf.preview`).

- Editor title button on `.tex`
- `Ctrl+Alt+V` to open PDF preview

This extension intentionally does **not** build. Keep build workflow in LaTeX Workshop.
